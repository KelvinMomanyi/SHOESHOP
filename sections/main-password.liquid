<section class="password-section">
  <div class="password-section__media" aria-hidden="true">
    {%- if section.settings.image != blank -%}
      {%- render 'responsive-image',
        image: section.settings.image,
        class: 'password-section__image',
        sizes: '100vw',
        loading: 'eager'
      -%}
    {%- else -%}
      {{ 'lifestyle-1' | placeholder_svg_tag: 'password-section__image placeholder-svg' }}
    {%- endif -%}
  </div>

  <div class="password-section__content">
    {%- if settings.logo != blank -%}
      {%- assign password_logo_focal_point = settings.logo.presentation.focal_point | default: 'center' -%}
      {%- assign password_logo_style = 'object-position: ' | append: password_logo_focal_point | append: ';' -%}
      {{ settings.logo | image_url: width: 320 | image_tag: widths: '160, 240, 320', class: 'password-section__logo', alt: shop.name, style: password_logo_style }}
    {%- else -%}
      <p class="eyebrow">{{ shop.name }}</p>
    {%- endif -%}
    <h1>{{ section.settings.heading }}</h1>
    {%- if shop.password_message != blank -%}
      <p>{{ shop.password_message }}</p>
    {%- elsif section.settings.text != blank -%}
      <p>{{ section.settings.text }}</p>
    {%- endif -%}

    {%- form 'storefront_password', class: 'password-form' -%}
      {{ form.errors | default_errors }}
      <label class="visually-hidden" for="Password">{{ 'customer.login.password' | t }}</label>
      <input id="Password" class="field__input" type="password" name="password" autocomplete="current-password" placeholder="{{ 'customer.login.password' | t }}">
      <button class="button" type="submit">{{ section.settings.button_label }}</button>
    {%- endform -%}
  </div>
</section>

{% schema %}
{
  "name": "t:sections.main_password.s001",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.main_password.s002"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "t:sections.main_password.s003",
      "default": "Opening soon"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "t:sections.main_password.s004",
      "default": "Enter the password to preview the store."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "t:sections.main_password.s005",
      "default": "Enter"
    }
  ]
}
{% endschema %}
