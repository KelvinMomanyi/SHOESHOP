<section class="contact-section section-band">
  <div class="page-width contact-section__inner">
    <div class="section-heading">
      <p class="eyebrow">{{ section.settings.kicker }}</p>
      <h1>{{ page.title | default: section.settings.heading }}</h1>
      {%- if section.settings.text != blank -%}
        <p>{{ section.settings.text }}</p>
      {%- endif -%}
    </div>

    {%- form 'contact', class: 'contact-form' -%}
      {%- if form.posted_successfully? -%}
        <p class="form-status">{{ 'general.contact.success' | t }}</p>
      {%- elsif form.errors -%}
        <div class="form-status form-status--error">{{ form.errors | default_errors }}</div>
      {%- endif -%}

      <div>
        <label for="ContactName-{{ section.id }}">{{ 'general.contact.name' | t }}</label>
        <input id="ContactName-{{ section.id }}" class="field__input" type="text" name="contact[name]" value="{% if form.name %}{{ form.name }}{% elsif customer %}{{ customer.name }}{% endif %}" autocomplete="name">
      </div>
      <div>
        <label for="ContactEmail-{{ section.id }}">{{ 'general.contact.email' | t }}</label>
        <input id="ContactEmail-{{ section.id }}" class="field__input" type="email" name="contact[email]" value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}" autocomplete="email" required>
      </div>
      <div>
        <label for="ContactMessage-{{ section.id }}">{{ 'general.contact.message' | t }}</label>
        <textarea id="ContactMessage-{{ section.id }}" class="field__input" name="contact[body]" rows="7" required>{{ form.body }}</textarea>
      </div>
      <button class="button" type="submit">{{ 'general.actions.send' | t }}</button>
    {%- endform -%}
  </div>
</section>

{% schema %}
{
  "name": "t:sections.contact_form.s001",
  "settings": [
    {
      "type": "text",
      "id": "kicker",
      "label": "t:sections.contact_form.s002",
      "default": "Contact"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "t:sections.contact_form.s003",
      "default": "Contact"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "t:sections.contact_form.s004",
      "default": "Questions, wholesale, sizing, or order support."
    }
  ],
  "presets": [
    {
      "name": "t:sections.contact_form.s005"
    }
  ]
}
{% endschema %}
