<section class="scroll-story section-band color-paper">
  <div class="scroll-story__intro page-width reveal-item">
    {%- if section.settings.kicker != blank -%}
      <p class="eyebrow">{{ section.settings.kicker }}</p>
    {%- endif -%}
    <h2>{{ section.settings.heading }}</h2>
    {%- if section.settings.text != blank -%}
      <p>{{ section.settings.text }}</p>
    {%- endif -%}
  </div>

  <div class="scroll-story__cards">
    {%- for block in section.blocks -%}
      {%- liquid
        assign story_product = block.settings.product
        assign story_image = block.settings.image | default: story_product.featured_media.preview_image | default: story_product.featured_image
        assign story_title = block.settings.title | default: story_product.title
        assign story_text = block.settings.text | default: story_product.description | strip_html | truncatewords: 32
      -%}
      <article class="story-card reveal-item" {{ block.shopify_attributes }}>
        <span class="story-card__number">({{ forloop.index | prepend: '0' | slice: -2, 2 }})</span>
        <div class="story-card__body">
          <h3>{{ story_title | default: 'Story title' }}</h3>
          <div class="story-card__media media-frame">
            {%- render 'responsive-image',
              image: story_image,
              class: 'media-frame__image',
              sizes: '(min-width: 990px) 55vw, 100vw'
            -%}
          </div>
          {%- if story_text != blank -%}
            <p>{{ story_text }}</p>
          {%- endif -%}
          {%- if story_product != blank -%}
            <a class="text-link" href="{{ story_product.url }}">{{ story_product.title }}</a>
          {%- elsif block.settings.link != blank -%}
            <a class="text-link" href="{{ block.settings.link }}">{{ block.settings.link_label | default: 'Explore' }}</a>
          {%- endif -%}
        </div>
      </article>
    {%- else -%}
      {%- for i in (1..3) -%}
        <article class="story-card reveal-item">
          <span class="story-card__number">({{ forloop.index | prepend: '0' | slice: -2, 2 }})</span>
          <div class="story-card__body">
            <h3>Editorial chapter</h3>
            <div class="story-card__media media-frame">
              {{ 'product-1' | placeholder_svg_tag: 'media-frame__image placeholder-svg' }}
            </div>
            <p>Use this block for collection stories, product craft notes, founder edits, or campaign drops.</p>
          </div>
        </article>
      {%- endfor -%}
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Scroll story",
  "settings": [
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker",
      "default": "Chapters"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "A shop you can scroll through"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Text",
      "default": "Numbered editorial modules give merchants a memorable way to pace launches, collections, and product storytelling."
    }
  ],
  "blocks": [
    {
      "type": "chapter",
      "name": "Chapter",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        },
        {
          "type": "text",
          "id": "link_label",
          "label": "Link label"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Scroll story",
      "blocks": [
        {
          "type": "chapter"
        },
        {
          "type": "chapter"
        },
        {
          "type": "chapter"
        }
      ]
    }
  ]
}
{% endschema %}
