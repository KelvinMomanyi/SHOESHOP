<section class="page-section section-band">
  <div class="page-width page-section__inner">
    <div class="section-heading">
      {%- if section.settings.kicker != blank -%}
        <p class="eyebrow">{{ section.settings.kicker }}</p>
      {%- endif -%}
      {%- if section.settings.show_title -%}
        <h1>{{ page.title }}</h1>
      {%- endif -%}
    </div>
    {%- if section.settings.image != blank -%}
      <div class="page-section__image media-frame">
        {%- render 'responsive-image',
          image: section.settings.image,
          class: 'media-frame__image',
          sizes: '(min-width: 990px) 80vw, 100vw',
          loading: 'eager'
        -%}
      </div>
    {%- endif -%}
    {%- if section.settings.show_content -%}
      <div class="rte page-section__content">
        {{ page.content }}
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "t:sections.main_page.s001",
  "settings": [
    {
      "type": "text",
      "id": "kicker",
      "label": "t:sections.main_page.s002",
      "default": "Page"
    },
    {
      "type": "checkbox",
      "id": "show_title",
      "label": "t:sections.main_page.s003",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_content",
      "label": "t:sections.main_page.s004",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.main_page.s005"
    }
  ]
}
{% endschema %}
